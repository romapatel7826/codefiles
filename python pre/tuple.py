# tuple does not support assigment , tuple is immutable
# we can not update value of tuple
# tuple items can not ulter and manupulate

t = (2, 3, 5, 6, 7, 8, 8)
#t = ()
#t = (1,) # comma is need in tuple with single element

print(t.count(8))
print(t.index(7))